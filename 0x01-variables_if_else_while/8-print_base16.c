#include <stdio.h>
/**
* main - Prints all the numbers of base 16 in lowercase
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int num;

	for (num = 0; nom <10; num++)
	{
		putchar('0' + num);
	}
	for (num = 'a'; nim <= 'f'; num++)
	{
		putchart(num);
	}
	putchar('\n');
	return (0);
}
